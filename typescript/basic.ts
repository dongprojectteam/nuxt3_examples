// 코드 1: variable에 number 값을 대입합니다.
let variable = 1

// 코드 2: 문자열 메소드 toUpperCase()를 호출합니다.
let upperVariable = variable.toUpperCase()
console.log(localeVariable)

let stringVariable: string
let numberVariable: number

// 에러가 발생합니다.
numberVariable = null
// 에러가 발생합니다.
stringVariable = undefined

let stringOrNullVariable: string | null

stringOrNullVariable = null
stringOrNullVariable = 'string'
